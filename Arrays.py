# Arrays are used when more than one values are to be stored in a single variable
# Arrays are handled in Python similar to a list and it has same methods
# Declaration, fetching a specific value, adding more values, deleting values, clearing array, deleting array all are similar to
# how it is done in case of list